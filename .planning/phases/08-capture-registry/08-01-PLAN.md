---
phase: 08-capture-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Backend/services/capture_service.py
  - Backend/tests/test_capture_registry.py
autonomous: true
requirements: [MCAP-03]
must_haves:
  truths:
    - "CaptureRegistry.acquire() creates a backend on first call and returns the same backend on subsequent calls for the same device path"
    - "CaptureRegistry.release() destroys the backend only when ref count reaches zero"
    - "Two zones acquiring different device paths get independent backends"
    - "Two zones sharing the same device path share one backend with ref count 2"
    - "CaptureRegistry.shutdown() releases all backends regardless of ref count"
  artifacts:
    - path: "Backend/services/capture_service.py"
      provides: "CaptureRegistry class with acquire/release/shutdown/get_default"
      contains: "class CaptureRegistry"
    - path: "Backend/tests/test_capture_registry.py"
      provides: "Unit tests for registry ref counting and lifecycle"
      min_lines: 80
  key_links:
    - from: "Backend/services/capture_service.py CaptureRegistry.acquire"
      to: "Backend/services/capture_service.py create_capture"
      via: "factory call on first acquisition"
      pattern: "create_capture\\(device_path\\)"
---

<objective>
Implement the CaptureRegistry class — a thread-safe, reference-counted pool of CaptureBackend instances keyed by device path. This is the core data structure that enables multi-camera capture.

Purpose: Provides the contract (acquire/release/shutdown/get_default) that StreamingService and routers will consume in Plan 02.
Output: CaptureRegistry class in capture_service.py + comprehensive unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-capture-registry/08-CONTEXT.md
@.planning/phases/08-capture-registry/08-RESEARCH.md

@Backend/services/capture_service.py
@Backend/tests/conftest.py

<interfaces>
<!-- Key types the executor needs from capture_service.py -->

From Backend/services/capture_service.py:
```python
CAPTURE_DEVICE: str = os.getenv("CAPTURE_DEVICE", _DEFAULT_DEVICE)

class CaptureBackend(abc.ABC):
    def __init__(self, device_path: str) -> None: ...
    @property
    def device_path(self) -> str: ...
    @property
    def is_open(self) -> bool: ...
    def open(self, device_path: Optional[str] = None) -> None: ...
    def release(self) -> None: ...
    async def get_frame(self) -> np.ndarray: ...
    async def get_jpeg(self) -> bytes: ...

def create_capture(device_path: str = CAPTURE_DEVICE) -> CaptureBackend: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CaptureRegistry class and unit tests</name>
  <files>Backend/services/capture_service.py, Backend/tests/test_capture_registry.py</files>
  <read_first>
    Backend/services/capture_service.py
    Backend/tests/conftest.py
    Backend/tests/test_capture_service.py
  </read_first>
  <behavior>
    - test_acquire_creates_backend: acquire("/dev/video0") calls create_capture("/dev/video0"), calls backend.open(), returns the backend
    - test_acquire_twice_same_device_returns_same_backend: acquire same path twice returns identical object, create_capture called once, open called once
    - test_acquire_different_devices_returns_different_backends: acquire "/dev/video0" and "/dev/video1" returns two distinct backends
    - test_release_at_zero_destroys_backend: acquire then release calls backend.release(), removes from internal dicts
    - test_two_zones_same_device_no_premature_release: acquire twice, release once — backend.release NOT called; release again — backend.release called once
    - test_release_nonexistent_device_is_noop: release("/dev/video99") does not raise
    - test_shutdown_releases_all: acquire two different devices, shutdown — both backend.release called, internal dicts empty
    - test_shutdown_empty_registry_is_noop: shutdown on fresh registry does not raise
    - test_get_default_returns_capture_device_backend: acquire CAPTURE_DEVICE, then get_default() returns that backend
    - test_get_default_returns_none_when_not_acquired: get_default() on fresh registry returns None
  </behavior>
  <action>
    **First write tests (RED), then implement (GREEN).**

    **Test file** (`Backend/tests/test_capture_registry.py`):
    - Import `CaptureRegistry` from `services.capture_service`
    - Use `unittest.mock.patch("services.capture_service.create_capture")` to mock the factory
    - Each test creates a fresh `CaptureRegistry()` instance
    - Mock backends are `MagicMock()` instances (no need for real V4L2)
    - Test the 10 behaviors listed above

    **Implementation** (append to `Backend/services/capture_service.py` after `create_capture`):
    - Add `class CaptureRegistry` per D-01 through D-06
    - Uses `threading.Lock` (NOT asyncio.Lock) per research recommendation — callers use `asyncio.to_thread` so methods run from thread-pool threads
    - Internal state: `self._backends: dict[str, CaptureBackend] = {}` and `self._ref_counts: dict[str, int] = {}`
    - `acquire(device_path: str) -> CaptureBackend`: Lock, check if device_path in _backends. If not: `backend = create_capture(device_path)`, `backend.open()`, store in _backends, set ref_count to 0. Increment ref_count. Return backend.
    - `release(device_path: str) -> None`: Lock, if device_path not in _ref_counts return (noop). Decrement. If <= 0: pop from both dicts, call `backend.release()`.
    - `get_default(self) -> CaptureBackend | None`: Lock, return `self._backends.get(CAPTURE_DEVICE)`.
    - `shutdown(self) -> None`: Lock, iterate _backends.values() calling `backend.release()` in try/except. Clear both dicts.
    - Update module docstring `Exports` section to include `CaptureRegistry`
  </action>
  <verify>
    <automated>source /tmp/hpc-venv/bin/activate && cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl/Backend && python -m pytest tests/test_capture_registry.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Backend/services/capture_service.py contains `class CaptureRegistry:`
    - Backend/services/capture_service.py contains `def acquire(self, device_path: str) -> CaptureBackend:`
    - Backend/services/capture_service.py contains `def release(self, device_path: str) -> None:`
    - Backend/services/capture_service.py contains `def shutdown(self) -> None:`
    - Backend/services/capture_service.py contains `def get_default(self) -> CaptureBackend | None:`
    - Backend/services/capture_service.py contains `self._lock = threading.Lock()`
    - Backend/tests/test_capture_registry.py contains at least 10 test functions (test_acquire_creates_backend, test_two_zones_same_device_no_premature_release, etc.)
    - `python -m pytest tests/test_capture_registry.py -x -q` exits 0 with 10+ tests passed
    - `python -m pytest --tb=short -q` exits 0 (full suite still green)
  </acceptance_criteria>
  <done>CaptureRegistry class exists with acquire/release/shutdown/get_default methods, all 10+ unit tests pass, full test suite green</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_capture_registry.py -x -q` — all registry tests pass
- `python -m pytest --tb=short -q` — full suite still green (no regressions from adding class)
- `grep "class CaptureRegistry" Backend/services/capture_service.py` — class exists
</verification>

<success_criteria>
- CaptureRegistry class with acquire/release/shutdown/get_default is importable from services.capture_service
- All 10+ unit tests pass covering ref counting, lifecycle, edge cases
- Full backend test suite (167+) remains green
</success_criteria>

<output>
After completion, create `.planning/phases/08-capture-registry/08-01-SUMMARY.md`
</output>
