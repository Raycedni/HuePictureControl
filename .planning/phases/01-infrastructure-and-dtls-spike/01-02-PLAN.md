---
phase: 01-infrastructure-and-dtls-spike
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - Backend/services/hue_client.py
  - Backend/routers/hue.py
  - Backend/models/hue.py
  - Backend/main.py
  - Backend/tests/test_hue_router.py
autonomous: true
requirements:
  - BRDG-01
  - BRDG-02
  - BRDG-03
  - BRDG-05

must_haves:
  truths:
    - "POST /api/hue/pair with valid bridge IP stores username + clientkey + all bridge metadata to SQLite"
    - "POST /api/hue/pair returns 403 when link button is not pressed"
    - "GET /api/hue/status returns current pairing state (paired/unpaired)"
    - "GET /api/hue/configs returns entertainment configurations from paired bridge"
    - "GET /api/hue/lights returns lights discovered from paired bridge"
    - "Credentials survive database close/reopen (persistence)"
  artifacts:
    - path: "Backend/services/hue_client.py"
      provides: "Bridge pairing, metadata fetch, config/light discovery"
      min_lines: 80
    - path: "Backend/routers/hue.py"
      provides: "REST endpoints for pairing and discovery"
      contains: "/api/hue"
    - path: "Backend/models/hue.py"
      provides: "Pydantic models for bridge credentials and configs"
      contains: "class BridgeCredentials"
  key_links:
    - from: "Backend/routers/hue.py"
      to: "Backend/services/hue_client.py"
      via: "import and call pairing/discovery functions"
      pattern: "from.*services.*hue_client.*import"
    - from: "Backend/routers/hue.py"
      to: "Backend/database.py"
      via: "request.app.state.db for credential storage"
      pattern: "request\\.app\\.state\\.db"
    - from: "Backend/main.py"
      to: "Backend/routers/hue.py"
      via: "app.include_router"
      pattern: "include_router.*hue"
---

<objective>
Implement bridge pairing, credential persistence, and entertainment configuration/light discovery endpoints.

Purpose: The bridge pairing flow is the prerequisite for the DTLS spike (Plan 04) and the UI pairing flow (Plan 03). This plan creates the backend API that both depend on.
Output: Working pairing endpoint that stores all bridge credentials to SQLite, discovery endpoints for configs and lights.
</objective>

<execution_context>
@/home/lukas/.claude/get-shit-done/workflows/execute-plan.md
@/home/lukas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-and-dtls-spike/01-RESEARCH.md

<interfaces>
<!-- From Plan 01 outputs — executor needs these contracts -->

From Backend/database.py:
```python
DATABASE_PATH: str  # configurable via env var
async def init_db(db_path: str) -> aiosqlite.Connection
async def close_db(db: aiosqlite.Connection)
# Tables: bridge_config (id, bridge_id, rid, ip_address, username, hue_app_id, client_key, swversion, name)
#          entertainment_configs (id, name, status, channel_count, raw_json)
#          regions, light_assignments
```

From Backend/main.py:
```python
app = FastAPI(lifespan=lifespan)
# app.state.db = aiosqlite.Connection (set during lifespan)
# Routers mounted via app.include_router()
```

From Backend/routers/health.py:
```python
router = APIRouter()
# GET /api/health -> {"status": "ok", "service": "HuePictureControl Backend"}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hue client service and Pydantic models</name>
  <files>Backend/models/hue.py, Backend/services/hue_client.py</files>
  <behavior>
    - test_pair_success: Mock requests.post to return [{"success": {"username": "test-user", "clientkey": "AABBCCDD"}}]. pair_with_bridge returns dict with username and clientkey.
    - test_pair_link_button_not_pressed: Mock requests.post to return [{"error": {"type": 101, "description": "link button not pressed"}}]. pair_with_bridge raises ValueError with "link button" in message.
    - test_fetch_bridge_metadata: Mock requests.get to return {"data": [{"bridge_id": "abc", "id": "rid-123", "owner": {"rid": "app-456"}, "swversion": 1968100080, "metadata": {"name": "My Bridge"}}]}. fetch_bridge_metadata returns dict with identification, rid, hue_app_id, swversion, name.
    - test_list_entertainment_configs: Mock httpx.AsyncClient.get to return {"data": [{"id": "cfg-1", "metadata": {"name": "TV"}, "status": "inactive", "channels": [{"channel_id": 0}, {"channel_id": 1}]}]}. list_entertainment_configs returns list with parsed config.
    - test_list_lights: Mock httpx.AsyncClient.get to return {"data": [{"id": "light-1", "metadata": {"name": "Strip"}, "type": "light"}]}. list_lights returns list with parsed light.
  </behavior>
  <action>
    1. Create Backend/models/hue.py with Pydantic models:
       - BridgeCredentials: bridge_id (str), rid (str), ip_address (str), username (str), hue_app_id (str), client_key (str), swversion (int), name (str)
       - PairRequest: bridge_ip (str)
       - PairResponse: status (str), bridge_ip (str), bridge_name (str)
       - EntertainmentConfigResponse: id (str), name (str), status (str), channel_count (int)
       - LightResponse: id (str), name (str), type (str)
       - BridgeStatusResponse: paired (bool), bridge_ip (str | None), bridge_name (str | None)

    2. Create Backend/services/hue_client.py:
       - def pair_with_bridge(bridge_ip: str) -> dict: POST https://{bridge_ip}/api with devicetype="HuePictureControl#backend" and generateclientkey=True. verify=False. Returns {"username": ..., "clientkey": ...}. Raises ValueError on error (e.g., link button not pressed). Suppress urllib3 InsecureRequestWarning.
       - def fetch_bridge_metadata(bridge_ip: str, username: str) -> dict: GET https://{bridge_ip}/clip/v2/resource/bridge with hue-application-key header. Returns dict with identification, rid, hue_app_id, swversion, name — per RESEARCH.md Pattern 3.
       - async def list_entertainment_configs(bridge_ip: str, username: str) -> list[dict]: GET https://{bridge_ip}/clip/v2/resource/entertainment_configuration. Returns list of dicts with id, name, status, channel_count (derived from len(channels)).
       - async def list_lights(bridge_ip: str, username: str) -> list[dict]: GET https://{bridge_ip}/clip/v2/resource/light. Returns list of dicts with id, name, type.
       - All HTTPS calls use verify=False (bridge uses self-signed cert).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl/Backend && python -m pytest tests/test_hue_router.py -x -v -k "pair or metadata or configs or lights"</automated>
  </verify>
  <done>All 5 behavior tests pass. Service functions handle success and error cases for pairing, metadata fetch, config listing, and light listing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hue REST endpoints with credential persistence</name>
  <files>Backend/routers/hue.py, Backend/main.py, Backend/tests/test_hue_router.py</files>
  <behavior>
    - test_pair_endpoint_success: POST /api/hue/pair with mock bridge -> returns 200 with status "paired", credentials stored in DB
    - test_pair_endpoint_link_button_error: POST /api/hue/pair when link button not pressed -> returns 403
    - test_pair_endpoint_bridge_unreachable: POST /api/hue/pair when bridge unreachable -> returns 502
    - test_status_unpaired: GET /api/hue/status with empty DB -> returns {"paired": false}
    - test_status_paired: GET /api/hue/status with credentials in DB -> returns {"paired": true, "bridge_ip": "...", "bridge_name": "..."}
    - test_list_configs_endpoint: GET /api/hue/configs with paired bridge -> returns list of entertainment configs
    - test_list_lights_endpoint: GET /api/hue/lights with paired bridge -> returns list of lights
    - test_list_configs_unpaired: GET /api/hue/configs with no credentials -> returns 400
  </behavior>
  <action>
    1. Create Backend/routers/hue.py:
       - APIRouter(prefix="/api/hue", tags=["hue"])
       - POST /pair: accepts PairRequest body. Calls pair_with_bridge(bridge_ip), then fetch_bridge_metadata(bridge_ip, username). Stores ALL fields to bridge_config table via INSERT OR REPLACE (single-row table, id=1). Returns PairResponse. On ValueError (link button): raise HTTPException(403). On connection error: raise HTTPException(502).
       - GET /status: queries bridge_config table for id=1. Returns BridgeStatusResponse with paired=True if row exists, False otherwise.
       - GET /configs: reads credentials from bridge_config. If no credentials, raise HTTPException(400, "Bridge not paired"). Calls list_entertainment_configs(ip, username). Returns list[EntertainmentConfigResponse].
       - GET /lights: same pattern as /configs but calls list_lights. Returns list[LightResponse].
       - All DB access via request.app.state.db

    2. Update Backend/main.py:
       - Import and mount hue router: app.include_router(hue_router)

    3. Update Backend/tests/test_hue_router.py (extending from Task 1):
       - Add integration tests using TestClient + in-memory DB
       - Mock out services/hue_client.py functions using unittest.mock.patch
       - Test full request/response cycle including DB persistence
       - test_pair_endpoint_success: verify credentials are actually in the DB after POST
       - test_status_paired/unpaired: verify correct JSON response shape
       - test_list_configs_unpaired: verify 400 response when not paired
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl/Backend && python -m pytest tests/ -x -v</automated>
  </verify>
  <done>All tests pass (database tests from Plan 01 + all hue router tests). POST /api/hue/pair stores credentials, GET /status reflects pairing state, GET /configs and /lights return discovery data from paired bridge.</done>
</task>

</tasks>

<verification>
- `cd Backend && python -m pytest tests/ -x -v` — all tests green
- POST /api/hue/pair stores all bridge fields (identification, rid, ip_address, username, hue_app_id, client_key, swversion, name)
- GET /api/hue/status correctly reports paired/unpaired
- GET /api/hue/configs and /api/hue/lights return data when paired, 400 when not
</verification>

<success_criteria>
- Bridge pairing endpoint accepts IP, calls bridge API, stores all credentials atomically to SQLite
- Link-button-not-pressed error returns 403 with clear message
- Entertainment config list endpoint returns configs with channel counts
- Light list endpoint returns lights with names and types
- All credentials survive DB close/reopen
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-and-dtls-spike/01-02-SUMMARY.md`
</output>
