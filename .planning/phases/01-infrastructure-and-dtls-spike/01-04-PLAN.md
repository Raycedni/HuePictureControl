---
phase: 01-infrastructure-and-dtls-spike
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - Backend/spike/dtls_test.py
  - Backend/spike/__init__.py
autonomous: false

requirements:
  - BRDG-05

must_haves:
  truths:
    - "A CLI script opens a DTLS session to the physical Hue Bridge and changes a real light's color"
    - "The script reads credentials from SQLite (stored by the pairing endpoint)"
    - "The script closes the DTLS session cleanly after the test"
  artifacts:
    - path: "Backend/spike/dtls_test.py"
      provides: "Standalone CLI DTLS spike script"
      min_lines: 40
  key_links:
    - from: "Backend/spike/dtls_test.py"
      to: "SQLite database"
      via: "reads bridge_config table for credentials"
      pattern: "SELECT.*FROM.*bridge_config"
    - from: "Backend/spike/dtls_test.py"
      to: "hue-entertainment-pykit"
      via: "create_bridge, Entertainment, Streaming"
      pattern: "from hue_entertainment_pykit import"
---

<objective>
Create and run the DTLS spike script that proves end-to-end communication with the physical Hue Bridge.

Purpose: This is the Phase 1 gate. The entire project depends on DTLS transport working. The spike must change a real light's color using hue-entertainment-pykit before any streaming work begins.
Output: Working spike/dtls_test.py script, verified against physical hardware.
</objective>

<execution_context>
@/home/lukas/.claude/get-shit-done/workflows/execute-plan.md
@/home/lukas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-and-dtls-spike/01-RESEARCH.md
@.planning/phases/01-infrastructure-and-dtls-spike/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01+02: database schema and hue_client service -->

SQLite bridge_config table:
  id INTEGER PRIMARY KEY
  bridge_id TEXT NOT NULL        -- maps to create_bridge(identification=...)
  rid TEXT NOT NULL               -- maps to create_bridge(rid=...)
  ip_address TEXT NOT NULL        -- maps to create_bridge(ip_address=...)
  username TEXT NOT NULL           -- maps to create_bridge(username=...)
  hue_app_id TEXT NOT NULL         -- maps to create_bridge(hue_app_id=...)
  client_key TEXT NOT NULL         -- maps to create_bridge(clientkey=...)
  swversion INTEGER NOT NULL       -- maps to create_bridge(swversion=...)
  name TEXT NOT NULL               -- maps to create_bridge(name=...)

From hue-entertainment-pykit:
  create_bridge(identification, rid, ip_address, swversion, username, hue_app_id, clientkey, name) -> Bridge
  Entertainment(bridge) -> service with get_entertainment_configs(), get_ent_conf_repo()
  Streaming(bridge, config, repo) -> streaming with start_stream(), set_color_space(), set_input(), stop_stream()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DTLS spike CLI script</name>
  <files>Backend/spike/__init__.py, Backend/spike/dtls_test.py</files>
  <action>
    1. Create empty Backend/spike/__init__.py

    2. Create Backend/spike/dtls_test.py as a standalone CLI script (python spike/dtls_test.py):
       - Parse optional arguments: --db-path (default: /app/data/config.db), --color (default: red), --duration (default: 3 seconds), --config-name (optional: specific entertainment config name to use)
       - Read credentials from SQLite bridge_config table (id=1). Use synchronous sqlite3 (this is a CLI script, not async).
       - If no credentials found, print error: "No bridge credentials found. Run the pairing flow first: POST /api/hue/pair"
       - Map credential columns to create_bridge() parameters:
         bridge_id -> identification, rid -> rid, ip_address -> ip_address,
         username -> username, hue_app_id -> hue_app_id, client_key -> clientkey,
         swversion -> swversion, name -> name
       - Create Bridge via create_bridge()
       - Create Entertainment service, get configs
       - If --config-name specified, find matching config; otherwise use first available
       - If no configs found, print error: "No entertainment configurations found. Create one in the Hue app."
       - Create Streaming, call start_stream()
       - Set color space to "xyb"
       - Define color presets: red=(0.675, 0.322, 0.8), green=(0.17, 0.7, 0.8), blue=(0.153, 0.048, 0.8), white=(0.3127, 0.3290, 1.0)
       - Send set_input with (x, y, brightness, channel_id=0) for the chosen color
       - Print: "Sent {color} to channel 0 — check your light!"
       - Sleep for --duration seconds
       - Call stop_stream()
       - Print: "DTLS spike complete. Stream closed."

    3. Add helpful comments explaining:
       - Why this script exists (Phase 1 gate: prove DTLS works)
       - That it must be run AFTER pairing via the web UI or API
       - That it must be run from the host network (or inside the backend container)
       - Open questions to verify: Does hue_app_id="" work? Does swversion=0 work? Log what values are actually used.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl/Backend && python -c "import spike.dtls_test; print('Module imports OK')"</automated>
  </verify>
  <done>spike/dtls_test.py exists, imports cleanly, reads credentials from SQLite, uses hue-entertainment-pykit to open DTLS session, sends a color, and closes cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify DTLS spike with physical Hue Bridge (Phase 1 gate)</name>
  <files>Backend/spike/dtls_test.py</files>
  <action>
    Human verifies that the DTLS spike script successfully changes a real light's color via the physical Hue Bridge. This is the Phase 1 gate — the entire project depends on this working.

    Prerequisites:
    - Physical Hue Bridge on the same LAN
    - At least one entertainment configuration created in the Hue app
    - At least one light assigned to the entertainment configuration

    Steps:
    1. Start the backend (locally or in Docker): cd Backend and run uvicorn main:app --host 0.0.0.0 --port 8000
    2. Press the link button on the Hue Bridge
    3. Pair via API: curl -X POST http://localhost:8000/api/hue/pair -H "Content-Type: application/json" -d '{"bridge_ip": "YOUR_BRIDGE_IP"}'
    4. Verify pairing: curl http://localhost:8000/api/hue/status (should show paired: true)
    5. Run the DTLS spike: cd Backend and run python -m spike.dtls_test --db-path data/config.db
    6. Observe: A physical light should change to RED for 3 seconds, then revert

    Expected outcome:
    - Terminal prints "Sent red to channel 0 — check your light!"
    - A real Hue light changes to red
    - After 3 seconds, terminal prints "DTLS spike complete. Stream closed."
    - The light returns to its previous state

    If it fails:
    - Check terminal output for error messages
    - Verify entertainment config exists: curl http://localhost:8000/api/hue/configs
    - Try with --color blue or --color green
    - Check if hue_app_id or swversion need real values (see open questions in RESEARCH.md)
  </action>
  <verify>Human observes physical light change color</verify>
  <done>A real Hue light changes to red (or chosen color) for the configured duration, then the stream closes cleanly. Phase 1 gate is passed. Type "approved" if the light changed color, or describe what happened.</done>
</task>

</tasks>

<verification>
- spike/dtls_test.py imports without error
- Script reads credentials from SQLite, creates Bridge, opens DTLS, sends color, closes
- CHECKPOINT: Physical light changes color (human verified)
</verification>

<success_criteria>
- A real Hue light changes color via the DTLS spike script (Phase 1 gate passed)
- Bridge credentials from SQLite are sufficient for hue-entertainment-pykit create_bridge()
- DTLS session opens and closes cleanly without errors
- Open questions about hue_app_id and swversion are resolved empirically
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-and-dtls-spike/01-04-SUMMARY.md`
</output>
