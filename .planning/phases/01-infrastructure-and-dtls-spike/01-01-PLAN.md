---
phase: 01-infrastructure-and-dtls-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yaml
  - Backend/Dockerfile
  - Backend/requirements.txt
  - Backend/main.py
  - Backend/database.py
  - Backend/routers/health.py
  - Backend/models/__init__.py
  - Backend/routers/__init__.py
  - Backend/services/__init__.py
  - Backend/tests/__init__.py
  - Backend/tests/conftest.py
  - Backend/tests/test_database.py
  - Backend/pytest.ini
autonomous: true
requirements:
  - INFR-01
  - INFR-02
  - INFR-03
  - INFR-05

must_haves:
  truths:
    - "docker compose up starts backend service on host network without errors"
    - "Backend /api/health returns 200 OK"
    - "SQLite database is created at /app/data/config.db on first startup with all tables"
    - "USB device passthrough is configured in docker-compose.yaml"
  artifacts:
    - path: "docker-compose.yaml"
      provides: "Two-service Docker Compose (backend host-network, frontend placeholder)"
      contains: "network_mode: host"
    - path: "Backend/Dockerfile"
      provides: "Python 3.12-slim backend image"
      contains: "python:3.12-slim"
    - path: "Backend/main.py"
      provides: "FastAPI app with lifespan, router mounts"
      exports: ["app"]
    - path: "Backend/database.py"
      provides: "aiosqlite schema init and DB access"
      contains: "CREATE TABLE IF NOT EXISTS"
    - path: "Backend/routers/health.py"
      provides: "Health check endpoint"
      contains: "/api/health"
    - path: "Backend/tests/conftest.py"
      provides: "Shared test fixtures (in-memory DB)"
  key_links:
    - from: "Backend/main.py"
      to: "Backend/database.py"
      via: "lifespan calls init_db, stores in app.state"
      pattern: "app\\.state\\.db"
    - from: "Backend/main.py"
      to: "Backend/routers/health.py"
      via: "app.include_router"
      pattern: "include_router"
    - from: "docker-compose.yaml"
      to: "Backend/Dockerfile"
      via: "build context"
      pattern: "context: ./Backend"
---

<objective>
Set up the Docker Compose infrastructure, FastAPI backend skeleton with aiosqlite database, and test scaffold.

Purpose: Everything in Phase 1 (and beyond) depends on a working Docker environment and backend skeleton. This plan creates the foundation.
Output: Working Docker Compose with backend on host network, FastAPI app with health endpoint, SQLite schema with all config tables, pytest test infrastructure.
</objective>

<execution_context>
@/home/lukas/.claude/get-shit-done/workflows/execute-plan.md
@/home/lukas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-and-dtls-spike/01-RESEARCH.md
@.planning/phases/01-infrastructure-and-dtls-spike/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FastAPI app skeleton with aiosqlite database and health endpoint</name>
  <files>Backend/main.py, Backend/database.py, Backend/routers/__init__.py, Backend/routers/health.py, Backend/models/__init__.py, Backend/services/__init__.py, Backend/requirements.txt, Backend/pytest.ini, Backend/tests/__init__.py, Backend/tests/conftest.py, Backend/tests/test_database.py</files>
  <behavior>
    - test_db_tables_created: After lifespan startup, tables bridge_config, entertainment_configs, regions, light_assignments exist in SQLite
    - test_db_file_created: Database file is created at configured path after init
    - test_credentials_persist: Insert a row into bridge_config, close DB, reopen, row is still there
    - test_health_endpoint: GET /api/health returns 200 with {"status": "ok"}
  </behavior>
  <action>
    1. Create Backend/requirements.txt with pinned dependencies:
       fastapi==0.115.6, uvicorn[standard]==0.32.1, hue-entertainment-pykit==0.9.3, aiosqlite==0.20.0, zeroconf==0.131.0, httpx==0.27.2, requests==2.32.3, python-multipart==0.0.18, pydantic==2.10.4, pytest==8.3.4, pytest-asyncio==0.24.0

    2. Create Backend/database.py:
       - DATABASE_PATH configurable via env var DATABASE_PATH, default "/app/data/config.db"
       - async def init_db(db_path) -> aiosqlite.Connection: opens connection, sets row_factory=aiosqlite.Row, creates 4 tables (bridge_config, entertainment_configs, regions, light_assignments) per the schema in RESEARCH.md Pattern 1, returns connection
       - async def close_db(db): closes the connection

    3. Create Backend/main.py:
       - FastAPI app with asynccontextmanager lifespan
       - Lifespan: calls init_db(), stores result in app.state.db, yields, calls close_db()
       - Mounts health router
       - Uvicorn entry point: if __name__ == "__main__": uvicorn.run("main:app", host="0.0.0.0", port=8000)

    4. Create Backend/routers/health.py:
       - APIRouter with GET /api/health returning {"status": "ok", "service": "HuePictureControl Backend"}

    5. Create empty __init__.py files for Backend/models/, Backend/routers/, Backend/services/

    6. Create Backend/pytest.ini with asyncio_mode = auto

    7. Create Backend/tests/conftest.py:
       - Fixture `db`: creates in-memory aiosqlite connection, runs init_db schema, yields, closes
       - Fixture `app_client`: creates FastAPI TestClient with lifespan override using temp file DB
       - Use pytest-asyncio for async fixtures

    8. Create Backend/tests/test_database.py:
       - test_db_tables_created: use db fixture, query sqlite_master for all 4 table names
       - test_credentials_persist: insert row into bridge_config, commit, query it back, assert fields match
       - test_db_file_created: use tmp_path, call init_db with tmp_path/test.db, assert file exists
       - test_health_endpoint: use app_client fixture, GET /api/health, assert 200 and JSON body
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl/Backend && pip install -r requirements.txt -q && python -m pytest tests/ -x -v</automated>
  </verify>
  <done>All 4 tests pass. FastAPI app starts, health endpoint returns 200, SQLite schema has all 4 tables, credentials survive insert/query cycle.</done>
</task>

<task type="auto">
  <name>Task 2: Docker Compose and backend Dockerfile</name>
  <files>docker-compose.yaml, Backend/Dockerfile</files>
  <action>
    1. Create Backend/Dockerfile:
       - FROM python:3.12-slim
       - Comment: "# PINNED: Python 3.12 required — hue-entertainment-pykit mbedTLS wheels break on 3.13+"
       - WORKDIR /app
       - COPY requirements.txt .
       - RUN pip install --no-cache-dir -r requirements.txt
       - COPY . .
       - RUN mkdir -p /app/data
       - CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]

    2. Create docker-compose.yaml (NO `version:` key — deprecated in current Compose spec):
       - Service `backend`:
         - build context: ./Backend
         - network_mode: host (INFR-03: required for DTLS/UDP and mDNS)
         - devices: ["/dev/video0:/dev/video0"] (INFR-02: USB passthrough)
         - group_add: [video] (needed for device access)
         - volumes: [hue_data:/app/data] (INFR-05: SQLite persistence)
         - environment: [LOG_LEVEL=INFO, DATABASE_PATH=/app/data/config.db]
         - restart: unless-stopped
         - healthcheck: CMD python -c "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health')", interval 30s, timeout 5s, retries 3, start_period 15s

       - Service `frontend` (placeholder — filled in Plan 03):
         - image: nginx:alpine
         - ports: ["80:80"]
         - extra_hosts: ["host.docker.internal:host-gateway"]
         - depends_on backend: condition service_healthy
         - restart: unless-stopped
         - Comment: "# Placeholder — Plan 03 replaces with Vite+React build"

       - volumes: hue_data:

    3. Create Backend/.dockerignore:
       - __pycache__, *.pyc, .pytest_cache, tests/, spike/, .env
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl && docker compose config --quiet 2>&1 && echo "Compose config valid"</automated>
  </verify>
  <done>docker-compose.yaml passes config validation. Backend Dockerfile uses python:3.12-slim. Host networking, USB passthrough, video group, SQLite volume, and healthcheck are all configured.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -v` in Backend/ — all tests green
- `docker compose config --quiet` — no errors
- docker-compose.yaml contains: network_mode: host, devices, group_add, volumes, healthcheck
- Backend/Dockerfile uses python:3.12-slim
</verification>

<success_criteria>
- FastAPI backend starts and serves /api/health returning 200
- SQLite database initializes with 4 tables (bridge_config, entertainment_configs, regions, light_assignments)
- Docker Compose config is valid with host networking, USB passthrough, and SQLite volume
- pytest infrastructure works with in-memory DB fixtures
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-and-dtls-spike/01-01-SUMMARY.md`
</output>
