---
phase: 04-frontend-canvas-editor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Backend/database.py
  - Backend/routers/regions.py
  - Backend/routers/preview_ws.py
  - Backend/main.py
  - Backend/tests/test_regions_router.py
  - Backend/tests/test_preview_ws.py
autonomous: true
requirements: [REGN-04, REGN-05, REGN-06]

must_haves:
  truths:
    - "POST /api/regions creates a region with polygon and optional light_id"
    - "PUT /api/regions/{id} updates polygon coords and/or light assignment"
    - "DELETE /api/regions/{id} removes a region"
    - "GET /api/regions returns regions with light_id field included"
    - "/ws/preview sends binary JPEG frames from the capture device"
  artifacts:
    - path: "Backend/routers/regions.py"
      provides: "Full region CRUD (POST, PUT, DELETE, GET)"
      exports: ["router"]
    - path: "Backend/routers/preview_ws.py"
      provides: "WebSocket preview endpoint"
      exports: ["router"]
    - path: "Backend/tests/test_regions_router.py"
      provides: "Tests for POST, PUT, DELETE endpoints"
    - path: "Backend/tests/test_preview_ws.py"
      provides: "Tests for /ws/preview binary streaming"
  key_links:
    - from: "Backend/routers/regions.py"
      to: "Backend/database.py"
      via: "request.app.state.db SQL queries"
      pattern: "db\\.execute.*regions"
    - from: "Backend/routers/preview_ws.py"
      to: "Backend/main.py"
      via: "app.state.capture for frame access"
      pattern: "app\\.state\\.capture"
    - from: "Backend/main.py"
      to: "Backend/routers/preview_ws.py"
      via: "app.include_router"
      pattern: "include_router.*preview_ws"
---

<objective>
Add backend region CRUD endpoints and WebSocket preview streaming to support the frontend canvas editor.

Purpose: The canvas editor needs to create/update/delete regions individually (not just auto-map), assign lights to regions, and show a live camera preview via WebSocket. These backend APIs must exist before the frontend can consume them.

Output: Extended regions router with POST/PUT/DELETE, new /ws/preview WebSocket endpoint, updated DB schema with light_id column on regions table.
</objective>

<execution_context>
@/home/lukas/.claude/get-shit-done/workflows/execute-plan.md
@/home/lukas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@Backend/routers/regions.py
@Backend/routers/streaming_ws.py
@Backend/database.py
@Backend/main.py
@Backend/models/hue.py
@Backend/services/capture_service.py
@Backend/tests/test_regions_router.py

<interfaces>
<!-- Existing interfaces the executor needs -->

From Backend/database.py:
```python
# regions table schema (current):
# id TEXT PRIMARY KEY, name TEXT, polygon TEXT NOT NULL, order_index INTEGER DEFAULT 0
# Need to ADD: light_id TEXT (nullable)
```

From Backend/routers/streaming_ws.py (pattern to follow for /ws/preview):
```python
@router.websocket("/ws/status")
async def ws_status(websocket: WebSocket):
    broadcaster = websocket.app.state.broadcaster
    await broadcaster.connect(websocket)
    try:
        while True:
            await websocket.receive_text()
    except WebSocketDisconnect:
        broadcaster.disconnect(websocket)
```

From Backend/services/capture_service.py:
```python
class LatestFrameCapture:
    async def get_frame(self) -> np.ndarray: ...  # Returns BGR frame
```

From Backend/routers/capture.py (JPEG encoding pattern):
```python
ok, buf = cv2.imencode(".jpg", frame, [cv2.IMWRITE_JPEG_QUALITY, 85])
return Response(content=buf.tobytes(), media_type="image/jpeg")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend regions router with CRUD + light_id and update DB schema</name>
  <files>Backend/database.py, Backend/routers/regions.py, Backend/tests/test_regions_router.py</files>
  <behavior>
    - POST /api/regions with {name, polygon, light_id?} returns 201 with created region including generated id
    - PUT /api/regions/{id} with {polygon?, light_id?, name?} returns 200 with updated region
    - PUT /api/regions/{id} with nonexistent id returns 404
    - DELETE /api/regions/{id} returns 204 on success
    - DELETE /api/regions/{id} with nonexistent id returns 404
    - GET /api/regions/ includes light_id field in each region object (null if unassigned)
    - POST creates region with auto-generated UUID id and next order_index
  </behavior>
  <action>
    1. In `database.py`: Add `light_id TEXT` column to regions CREATE TABLE (after order_index). Since this uses CREATE TABLE IF NOT EXISTS and the DB may already have data, also add an ALTER TABLE migration: `ALTER TABLE regions ADD COLUMN light_id TEXT`. Wrap in try/except to handle case where column already exists (OperationalError).

    2. In `regions.py`: Add Pydantic models:
       - `CreateRegionRequest(BaseModel)`: name (str), polygon (list[list[float]]), light_id (str | None = None)
       - `UpdateRegionRequest(BaseModel)`: name (str | None = None), polygon (list[list[float]] | None = None), light_id (str | None = None)

    3. Add POST /api/regions (not POST /) endpoint:
       - Generate UUID via `uuid.uuid4()` as string id
       - Get max order_index from DB, set new = max + 1
       - INSERT INTO regions (id, name, polygon, order_index, light_id)
       - Return 201 with the created region object

    4. Add PUT /api/regions/{region_id}:
       - Check region exists (SELECT), return 404 if not
       - Build UPDATE SET clause dynamically from non-None fields
       - Serialize polygon as JSON string if provided
       - Return 200 with updated region object

    5. Add DELETE /api/regions/{region_id}:
       - Check region exists, return 404 if not
       - DELETE FROM regions WHERE id=?
       - Also DELETE FROM light_assignments WHERE region_id=? (cleanup)
       - Return 204

    6. Update existing GET / endpoint to include light_id in the response.

    7. Write tests in test_regions_router.py following existing pattern (make_test_app with lifespan + in-memory DB):
       - test_create_region_returns_201
       - test_update_region_polygon
       - test_update_region_light_id
       - test_update_nonexistent_returns_404
       - test_delete_region_returns_204
       - test_delete_nonexistent_returns_404
       - test_list_regions_includes_light_id
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl/Backend && python -m pytest tests/test_regions_router.py -x -v</automated>
  </verify>
  <done>All 7+ new tests pass. POST creates regions, PUT updates them, DELETE removes them, GET returns light_id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add /ws/preview WebSocket endpoint for binary JPEG streaming</name>
  <files>Backend/routers/preview_ws.py, Backend/main.py, Backend/tests/test_preview_ws.py</files>
  <behavior>
    - /ws/preview accepts WebSocket connection and sends binary JPEG frames
    - Frames are sent at ~10 fps (100ms sleep between frames)
    - If capture device is unavailable, sends nothing (no crash)
    - WebSocket disconnects cleanly on client close
    - Multiple clients can connect simultaneously
  </behavior>
  <action>
    1. Create `Backend/routers/preview_ws.py`:
       - WebSocket endpoint at `/ws/preview`
       - On connect: accept the WebSocket
       - Loop: call `capture.get_frame()`, encode as JPEG (quality 70 for speed), send as binary message, sleep 0.1s (10 fps)
       - Wrap get_frame in try/except RuntimeError — if device unavailable, sleep 1s and retry (don't close WS)
       - On WebSocketDisconnect: break loop, clean exit
       - Access capture via `websocket.app.state.capture`

    2. In `Backend/main.py`:
       - Import and include the new router: `from routers.preview_ws import router as preview_ws_router`
       - `app.include_router(preview_ws_router)`

    3. Create `Backend/tests/test_preview_ws.py`:
       - Test that /ws/preview accepts connection and sends binary data
       - Mock capture.get_frame() to return a numpy array
       - Test that disconnecting client doesn't crash server
       - Follow existing test patterns from test_streaming_ws.py
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl/Backend && python -m pytest tests/test_preview_ws.py -x -v</automated>
  </verify>
  <done>/ws/preview endpoint sends binary JPEG frames from the capture device. Tests pass. Router is wired in main.py.</done>
</task>

</tasks>

<verification>
```bash
cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl/Backend && python -m pytest tests/test_regions_router.py tests/test_preview_ws.py -x -v
```
All backend tests for new CRUD endpoints and preview WebSocket pass.
</verification>

<success_criteria>
- POST/PUT/DELETE region endpoints work with proper status codes
- GET /api/regions includes light_id field
- /ws/preview streams binary JPEG frames from capture device
- All new tests pass
- DB schema includes light_id column on regions table
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-canvas-editor/04-01-SUMMARY.md`
</output>
