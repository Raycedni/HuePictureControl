---
phase: 03.1-auto-mapping-from-entertainment-config
plan: 02
subsystem: ui
tags: [react, typescript, vite, fetch, regions, auto-map]

# Dependency graph
requires:
  - phase: 03.1-auto-mapping-from-entertainment-config
    provides: backend regions router with /api/regions and /api/regions/auto-map endpoints
provides:
  - PreviewPage component with snapshot display, region overlays, auto-map trigger, start/stop toggle
  - regions.ts API client with fetchRegions, triggerAutoMap, fetchConfigs, startStreaming, stopStreaming
  - Tab navigation in App.tsx between Setup and Preview pages
affects: [future frontend phases, any UI that extends PreviewPage]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Cache-busting snapshot refresh via ?t={Date.now()} on 2-second interval
    - Normalized polygon coordinates (0-1) multiplied by rendered img dimensions for overlay positioning
    - useState<'setup'|'preview'> tab switching without a routing library

key-files:
  created:
    - Frontend/src/api/regions.ts
    - Frontend/src/components/PreviewPage.tsx
  modified:
    - Frontend/src/App.tsx

key-decisions:
  - "Overlay positioning uses rendered clientWidth/clientHeight (not naturalWidth/naturalHeight) so overlays track the displayed image regardless of CSS scaling"
  - "Region overlays use first two polygon points as top-left and bottom-right corners, consistent with auto-map square region convention"
  - "Streaming start/stop reuses existing /api/capture/start and /api/capture/stop endpoints from capture router"

patterns-established:
  - "Camera snapshot refresh: setInterval(2000) updating a state timestamp used as ?t= query param"
  - "Semi-transparent region overlays: absolutely-positioned divs over an inline-block img container"

requirements-completed:
  - REGN-04
  - REGN-05

# Metrics
duration: 10min
completed: 2026-03-24
---

# Phase 03.1 Plan 02: Preview Page with Region Overlays Summary

**React PreviewPage with camera snapshot, semi-transparent region overlays, entertainment config auto-map trigger, and start/stop streaming toggle wired to existing backend endpoints**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-03-24T20:30:00Z
- **Completed:** 2026-03-24T20:40:00Z
- **Tasks:** 1 auto + 1 human-verify checkpoint
- **Files modified:** 3

## Accomplishments

- Created `Frontend/src/api/regions.ts` with five typed API functions covering all region and streaming endpoints
- Created `Frontend/src/components/PreviewPage.tsx`: config dropdown, auto-map button with status feedback, 2-second-refresh snapshot with absolutely-positioned overlay divs, start/stop streaming toggle
- Updated `Frontend/src/App.tsx` with two-tab navigation (Setup / Preview) using state-based page switching

## Task Commits

1. **Task 1: API client + PreviewPage component + App.tsx navigation** - `af97286` (feat)

**Plan metadata:** pending docs commit

## Files Created/Modified

- `Frontend/src/api/regions.ts` - Typed API client: fetchRegions, triggerAutoMap, fetchConfigs, startStreaming, stopStreaming
- `Frontend/src/components/PreviewPage.tsx` - Preview page with snapshot, region overlays, auto-map controls, streaming toggle
- `Frontend/src/App.tsx` - Tab navigation between Setup (PairingFlow) and Preview (PreviewPage)

## Decisions Made

- Overlay positioning uses `clientWidth`/`clientHeight` instead of `naturalWidth`/`naturalHeight` so div sizes correctly match the rendered (possibly scaled) image.
- Region color palette cycles through 8 predefined semi-transparent colors to visually distinguish overlapping regions.
- Auto-map status and error are shown inline next to the button rather than as a separate toast/modal, keeping the layout minimal.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Complete auto-mapping pipeline (backend + frontend) is ready for end-to-end hardware verification
- PreviewPage loads existing regions on mount, satisfying the persistence check (REGN-05)
- Tab navigation in place; future phases can add more tabs without refactoring

---
*Phase: 03.1-auto-mapping-from-entertainment-config*
*Completed: 2026-03-24*
