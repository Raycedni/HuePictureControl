---
phase: 03.1-auto-mapping-from-entertainment-config
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - Frontend/src/App.tsx
  - Frontend/src/components/PreviewPage.tsx
  - Frontend/src/api/regions.ts
autonomous: false
requirements:
  - REGN-04
  - REGN-05

must_haves:
  truths:
    - "Preview page shows camera snapshot with auto-generated region overlays"
    - "User can select an entertainment config and trigger auto-mapping from the UI"
    - "Auto-generated regions are visually displayed as semi-transparent rectangles on the camera feed"
    - "Start/stop streaming toggle is available and wired to existing /api/capture/start and /api/capture/stop"
  artifacts:
    - path: "Frontend/src/components/PreviewPage.tsx"
      provides: "Preview page with snapshot image, region overlays, auto-map trigger, start/stop toggle"
      min_lines: 80
    - path: "Frontend/src/api/regions.ts"
      provides: "API client functions for regions and auto-map endpoints"
      exports: ["fetchRegions", "triggerAutoMap"]
    - path: "Frontend/src/App.tsx"
      provides: "Tab/page navigation between PairingFlow and PreviewPage"
  key_links:
    - from: "Frontend/src/components/PreviewPage.tsx"
      to: "/api/regions/auto-map"
      via: "POST fetch call from triggerAutoMap"
      pattern: "api/regions/auto-map"
    - from: "Frontend/src/components/PreviewPage.tsx"
      to: "/api/capture/snapshot"
      via: "img src for camera feed"
      pattern: "api/capture/snapshot"
    - from: "Frontend/src/components/PreviewPage.tsx"
      to: "/api/regions"
      via: "GET fetch to display region overlays"
      pattern: "api/regions"
---

<objective>
Build a minimal preview page that shows the camera feed with auto-generated region overlays, an entertainment config selector with auto-map trigger, and a start/stop streaming toggle.

Purpose: Provides visual confirmation that auto-mapping works correctly and enables the user to operate the full streaming pipeline from the UI.
Output: PreviewPage component, API client, updated App.tsx with page navigation.
</objective>

<execution_context>
@/home/lukas/.claude/get-shit-done/workflows/execute-plan.md
@/home/lukas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-auto-mapping-from-entertainment-config/03.1-RESEARCH.md
@.planning/phases/03.1-auto-mapping-from-entertainment-config/03.1-01-SUMMARY.md

@Frontend/src/App.tsx
@Frontend/src/components/PairingFlow.tsx
@Backend/routers/regions.py
@Backend/routers/capture.py

<interfaces>
<!-- Backend API endpoints available (from Plan 01 + existing) -->

```
GET  /api/capture/snapshot        -> JPEG image (camera frame)
POST /api/capture/start           -> {"status": "starting"}  (body: {"config_id": "..."})
POST /api/capture/stop            -> {"status": "stopping"}
GET  /api/hue/configs             -> [{id, name, status, channel_count}]
GET  /api/hue/status              -> {paired: bool, bridge_ip?, bridge_name?}
POST /api/regions/auto-map        -> {"regions_created": N, "warning"?: "..."}  (body: {"config_id": "..."})
GET  /api/regions                 -> [{id, name, polygon: [[x,y],...], order_index}]
```

<!-- Existing Frontend patterns (from App.tsx, PairingFlow.tsx) -->
- React 19 + TypeScript + Vite
- No routing library — use simple state-based page switching
- Fetch-based API calls (no axios)
- Existing PairingFlow component handles bridge pairing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API client + PreviewPage component + App.tsx navigation</name>
  <files>Frontend/src/api/regions.ts, Frontend/src/components/PreviewPage.tsx, Frontend/src/App.tsx</files>
  <action>
    **Create** `Frontend/src/api/regions.ts`:
    - `fetchRegions(): Promise<Region[]>` — GET /api/regions, returns parsed JSON
    - `triggerAutoMap(configId: string): Promise<{regions_created: number, warning?: string}>` — POST /api/regions/auto-map
    - `fetchConfigs(): Promise<Config[]>` — GET /api/hue/configs (reuse if exists, otherwise create)
    - `startStreaming(configId: string): Promise<void>` — POST /api/capture/start
    - `stopStreaming(): Promise<void>` — POST /api/capture/stop
    - Type definitions: `Region = {id: string, name: string, polygon: number[][], order_index: number}`, `Config = {id: string, name: string, status: string, channel_count: number}`

    **Create** `Frontend/src/components/PreviewPage.tsx`:
    - **Config selector**: Dropdown populated from `GET /api/hue/configs`. "Auto-Map" button next to it that calls `POST /api/regions/auto-map` with selected config_id. Show `regions_created` count and any warning in a status message.
    - **Camera preview**: `<img>` tag with `src="/api/capture/snapshot"` that refreshes every 2 seconds (setInterval updating a cache-busting query param `?t={Date.now()}`). Show "No capture device" message if image fails to load.
    - **Region overlay**: After auto-map (or on page load), fetch regions from `GET /api/regions`. Render semi-transparent colored rectangles as absolutely-positioned divs over the camera image, using the normalized polygon coordinates (multiply by image dimensions). Use the first two points of each polygon (top-left and bottom-right of the square) to position/size the div. Show region name as a label.
    - **Start/Stop toggle**: Button that calls POST /api/capture/start (with selected config_id) or POST /api/capture/stop. Track streaming state locally (idle/streaming). Disable auto-map and config selector while streaming.
    - **Layout**: Stack vertically — config selector row at top, camera preview with overlays in center, start/stop button at bottom. Keep styling minimal (inline styles or simple CSS classes in App.css).

    **Update** `Frontend/src/App.tsx`:
    - Add simple tab navigation: "Setup" tab (shows PairingFlow) and "Preview" tab (shows PreviewPage)
    - Use `useState<'setup' | 'preview'>('setup')` for page switching
    - Render tab buttons at the top, active tab highlighted
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl/Frontend && npx tsc --noEmit</automated>
  </verify>
  <done>PreviewPage component renders with config selector, auto-map button, camera preview with region overlays, and start/stop toggle. App.tsx has tab navigation between Setup and Preview pages. TypeScript compiles without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end auto-mapping pipeline with real hardware</name>
  <files>n/a</files>
  <action>
    Human verifies the complete auto-mapping pipeline works end-to-end with real Hue Bridge and capture card hardware.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl/Frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Human confirms: auto-map generates correct regions, preview shows overlays, streaming works with auto-mapped regions, regions persist across refresh.</done>
  <what-built>Complete auto-mapping pipeline: backend service that converts entertainment config channel positions to screen regions, REST endpoints, and a preview page showing camera feed with region overlays and streaming controls.</what-built>
  <how-to-verify>
    1. Start the backend: `cd Backend && python -m main` (or via Docker)
    2. Open the frontend in browser (http://localhost:5173 or via Docker)
    3. Navigate to the "Preview" tab
    4. Select an entertainment configuration from the dropdown
    5. Click "Auto-Map" — verify it reports regions created
    6. Verify colored rectangles appear over the camera preview image, positioned according to light positions
    7. Click "Start" — verify streaming begins (lights should respond to camera colors)
    8. Click "Stop" — verify streaming stops cleanly
    9. Refresh the page — verify regions are still shown (persistence check, REGN-05)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
```bash
# TypeScript compiles
cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl/Frontend && npx tsc --noEmit

# Full backend test suite still passes
cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl/Backend && python -m pytest -x -q
```
</verification>

<success_criteria>
- Preview page displays camera snapshot with auto-generated region overlays
- Selecting a config and clicking Auto-Map creates regions visible on the preview
- Start/Stop toggle controls the streaming pipeline
- Regions survive page refresh (read from persisted SQLite data)
- Human verification confirms end-to-end flow works with real hardware
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-auto-mapping-from-entertainment-config/03.1-02-SUMMARY.md`
</output>
