---
phase: 02-capture-pipeline-color-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - Backend/routers/capture.py
  - Backend/main.py
  - Backend/tests/test_capture_router.py
  - Backend/tests/conftest.py
autonomous: false
requirements:
  - CAPT-02
  - CAPT-05

must_haves:
  truths:
    - "GET /api/capture/snapshot returns a valid JPEG image from the capture device"
    - "GET /api/capture/snapshot returns HTTP 503 when no capture device is available"
    - "PUT /api/capture/device switches the capture device path without restarting the container"
    - "PUT /api/capture/device returns HTTP 503 when the new device path is invalid"
    - "LatestFrameCapture is initialized in the FastAPI lifespan from CAPTURE_DEVICE env var"
    - "LatestFrameCapture is released on application shutdown"
    - "A debug endpoint or log shows extracted CIE xy color for a hard-coded test region"
  artifacts:
    - path: "Backend/routers/capture.py"
      provides: "GET /api/capture/snapshot and PUT /api/capture/device endpoints"
      exports: ["router"]
    - path: "Backend/main.py"
      provides: "Lifespan wiring of LatestFrameCapture on app.state.capture"
    - path: "Backend/tests/test_capture_router.py"
      provides: "Router-level tests with mocked capture service"
  key_links:
    - from: "Backend/routers/capture.py"
      to: "Backend/services/capture_service.py"
      via: "request.app.state.capture"
      pattern: "request\\.app\\.state\\.capture"
    - from: "Backend/routers/capture.py"
      to: "cv2.imencode"
      via: "JPEG encoding of captured frame"
      pattern: "cv2\\.imencode"
    - from: "Backend/main.py"
      to: "Backend/services/capture_service.py"
      via: "lifespan instantiation and open/release"
      pattern: "LatestFrameCapture"
---

<objective>
Wire the capture service into FastAPI with REST endpoints and lifespan management.

Purpose: Expose the capture pipeline via REST API so the frontend (Phase 4) and debug tools can access camera snapshots and reconfigure the device. This completes the Phase 2 deliverables.

Output: `capture.py` router with snapshot and device endpoints, updated `main.py` lifespan, router tests, and a debug color extraction log on snapshot.
</objective>

<execution_context>
@/home/lukas/.claude/get-shit-done/workflows/execute-plan.md
@/home/lukas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-capture-pipeline-color-extraction/02-RESEARCH.md
@.planning/phases/02-capture-pipeline-color-extraction/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — the contracts this plan consumes -->

From Backend/services/capture_service.py (created in Plan 01):
```python
class LatestFrameCapture:
    def __init__(self, device_path: str = "/dev/video0"): ...
    def open(self, device_path: Optional[str] = None) -> None: ...
    def release(self) -> None: ...
    async def get_frame(self) -> np.ndarray: ...
    @property
    def device_path(self) -> str: ...
```

From Backend/services/color_math.py (created in Plan 01):
```python
def rgb_to_xy(r: int, g: int, b: int) -> tuple[float, float]: ...
def build_polygon_mask(normalized_points: list[list[float]], width: int = 640, height: int = 480) -> np.ndarray: ...
def extract_region_color(frame: np.ndarray, mask: np.ndarray) -> tuple[int, int, int]: ...
```

From Backend/main.py (existing):
```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    db = await init_db(DATABASE_PATH)
    app.state.db = db
    yield
    await close_db(db)

app = FastAPI(lifespan=lifespan)
app.include_router(health_router)
app.include_router(hue_router)
```

From Backend/tests/conftest.py (existing test patterns):
```python
@pytest.fixture
def app_client(tmp_path):
    """FastAPI TestClient with lifespan using a temp file DB."""
    # Creates test_app with test_lifespan, yields TestClient
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create capture router with tests and wire into lifespan</name>
  <files>Backend/routers/capture.py, Backend/main.py, Backend/tests/test_capture_router.py, Backend/tests/conftest.py</files>
  <behavior>
    - GET /api/capture/snapshot returns 200 with Content-Type image/jpeg when device is open
    - GET /api/capture/snapshot returns 503 when capture device is not available (_cap is None)
    - PUT /api/capture/device with {"device_path": "/dev/video1"} returns 200 with new path
    - PUT /api/capture/device with invalid path returns 503
    - GET /api/capture/debug/color returns JSON with CIE xy values for a hard-coded center region
    - Snapshot response body is valid JPEG bytes (starts with FFD8)
  </behavior>
  <action>
    1. Create `Backend/tests/test_capture_router.py`:
       - Add a `capture_app_client` fixture to `Backend/tests/conftest.py` that creates a test FastAPI app with a mocked `LatestFrameCapture` on `app.state.capture`. The mock's `get_frame` returns a solid-color numpy array (e.g., 480x640x3 all blue). Use `unittest.mock.AsyncMock` for `get_frame`.
       - Test `GET /api/capture/snapshot` returns 200, content-type is image/jpeg, body starts with `b'\xff\xd8'` (JPEG magic bytes).
       - Test `GET /api/capture/snapshot` returns 503 when `get_frame` raises RuntimeError.
       - Test `PUT /api/capture/device` with valid path calls `open(new_path)` and returns 200.
       - Test `PUT /api/capture/device` returns 503 when `open()` raises RuntimeError.
       - Test `GET /api/capture/debug/color` returns JSON with `xy` field containing two floats.

    2. Run tests — they must FAIL.

    3. Create `Backend/routers/capture.py`:
       - `router = APIRouter(prefix="/api/capture", tags=["capture"])`
       - `GET /snapshot`: get frame via `request.app.state.capture.get_frame()`, encode with `cv2.imencode(".jpg", frame, [cv2.IMWRITE_JPEG_QUALITY, 85])`, return `Response(content=buf.tobytes(), media_type="image/jpeg")`. Catch RuntimeError -> HTTPException(503).
       - `PUT /device`: accept `SetDeviceRequest(device_path: str)` body. Call `capture_service.open(body.device_path)`. Catch RuntimeError -> HTTPException(503). Return `{"device_path": ..., "status": "opened"}`.
       - `GET /debug/color`: capture a frame, build a hard-coded center polygon mask ([[0.25, 0.25], [0.75, 0.25], [0.75, 0.75], [0.25, 0.75]]), extract region color, convert to CIE xy, return JSON `{"rgb": [r,g,b], "xy": [x,y]}`. This satisfies Phase 2 Success Criterion 3 (debug log/endpoint showing CIE xy). Import from `services.color_math`.
       - Use `import logging; logger = logging.getLogger(__name__)` for debug logging of actual fourcc and frame timing.

    4. Update `Backend/main.py`:
       - Add `import os` and `from routers.capture import router as capture_router`
       - Add `CAPTURE_DEVICE = os.getenv("CAPTURE_DEVICE", "/dev/video0")`
       - In lifespan startup (after db init): instantiate `LatestFrameCapture(CAPTURE_DEVICE)`, try `capture.open()` in a try/except (log warning on failure, don't crash — device may be absent in dev/test), set `app.state.capture = capture`.
       - In lifespan shutdown (before db close): call `capture.release()`.
       - Add `app.include_router(capture_router)`.

    5. Run tests — they must PASS.

    6. Run full test suite to verify no regressions.

    IMPORTANT: The lifespan must NOT crash if the capture device is absent. Log a warning and continue. The snapshot endpoint returns 503 in that case. This makes the backend testable and runnable without hardware.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl/Backend && python -m pytest tests/ -x -v</automated>
  </verify>
  <done>All router tests pass. GET /api/capture/snapshot returns JPEG on success and 503 on failure. PUT /api/capture/device switches device. GET /api/capture/debug/color returns CIE xy. main.py lifespan creates and releases LatestFrameCapture. Full test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify capture pipeline endpoints with and without hardware</name>
  <files>Backend/routers/capture.py, Backend/main.py</files>
  <action>
    Human verifies that the capture pipeline endpoints work correctly. This covers both the hardware-present and hardware-absent scenarios.

    What was built: Complete capture pipeline including LatestFrameCapture service, color math (RGB to CIE xy with Gamut C), snapshot endpoint, device config endpoint, and debug color endpoint. All backed by comprehensive unit tests with mocked cv2.

    Verification steps:
    1. Start the backend (with or without capture card):
       cd Backend && python -m main
    2. If capture card is connected:
       - Visit http://localhost:8000/api/capture/snapshot — should return a JPEG image
       - Visit http://localhost:8000/api/capture/debug/color — should return JSON with rgb and xy fields
       - Verify snapshot responds within 200ms (browser dev tools network tab)
    3. If capture card is NOT connected:
       - Backend should start without crashing (check logs for warning about capture device)
       - http://localhost:8000/api/capture/snapshot should return HTTP 503
       - http://localhost:8000/api/health should still return 200
    4. Test device reconfiguration:
       - curl -X PUT http://localhost:8000/api/capture/device -H "Content-Type: application/json" -d '{"device_path": "/dev/video99"}' — should return 503 (invalid device)
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl/Backend && python -m pytest tests/ -x -v</automated>
  </verify>
  <done>Human confirms: backend starts cleanly, snapshot endpoint returns JPEG (or 503 without hardware), debug/color returns CIE xy values, device reconfiguration endpoint works. Phase 2 complete.</done>
</task>

</tasks>

<verification>
Run the full test suite:
```bash
cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl/Backend && python -m pytest tests/ -x -v
```

Phase 2 success criteria verification:
1. GET /api/capture/snapshot returns valid JPEG — verified by test_snapshot_returns_jpeg
2. PUT /api/capture/device changes device path without restart — verified by test_set_device_reopens
3. GET /api/capture/debug/color shows CIE xy values — verified by test_debug_color_returns_xy
</verification>

<success_criteria>
- `Backend/routers/capture.py` exists with snapshot, device, and debug/color endpoints
- `Backend/main.py` lifespan manages LatestFrameCapture lifecycle
- Backend starts without crashing when capture device is absent
- All tests pass including Phase 1 tests (no regressions)
- Human verification confirms endpoints work (with or without hardware)
</success_criteria>

<output>
After completion, create `.planning/phases/02-capture-pipeline-color-extraction/02-02-SUMMARY.md`
</output>
