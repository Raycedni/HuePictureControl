---
phase: 02-capture-pipeline-color-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Backend/requirements.txt
  - Backend/services/capture_service.py
  - Backend/services/color_math.py
  - Backend/tests/test_capture_service.py
  - Backend/tests/test_color_math.py
autonomous: true
requirements:
  - CAPT-01
  - CAPT-02

must_haves:
  truths:
    - "LatestFrameCapture opens a V4L2 device at 640x480 MJPEG via cv2.VideoCapture"
    - "get_frame() delegates blocking cap.read() to asyncio.to_thread so the event loop is not blocked"
    - "LatestFrameCapture can be re-opened with a different device path at runtime"
    - "rgb_to_xy converts sRGB to CIE xy with Gamut C clamping and handles black input without error"
    - "build_polygon_mask creates a binary uint8 mask from normalized [0..1] polygon coordinates"
    - "extract_region_color returns mean RGB from a frame within a polygon mask"
  artifacts:
    - path: "Backend/services/capture_service.py"
      provides: "LatestFrameCapture class with open/release/get_frame"
      exports: ["LatestFrameCapture"]
    - path: "Backend/services/color_math.py"
      provides: "RGB to CIE xy conversion, polygon mask building, region color extraction"
      exports: ["rgb_to_xy", "build_polygon_mask", "extract_region_color"]
    - path: "Backend/tests/test_capture_service.py"
      provides: "Unit tests for LatestFrameCapture with mocked cv2"
    - path: "Backend/tests/test_color_math.py"
      provides: "Unit tests for color math functions"
  key_links:
    - from: "Backend/services/capture_service.py"
      to: "cv2.VideoCapture"
      via: "V4L2 backend with MJPG fourcc at 640x480"
      pattern: "cv2\\.VideoCapture\\(.*CAP_V4L2\\)"
    - from: "Backend/services/capture_service.py"
      to: "asyncio.to_thread"
      via: "non-blocking wrapper around cap.read()"
      pattern: "asyncio\\.to_thread"
    - from: "Backend/services/color_math.py"
      to: "cv2.fillPoly"
      via: "polygon mask creation"
      pattern: "cv2\\.fillPoly"
---

<objective>
Create the capture service layer and color math module for the frame capture pipeline.

Purpose: Establish the core service classes that all capture-related endpoints and the future streaming loop (Phase 3) depend on. This is the foundation layer — pure logic, fully testable with mocked cv2.

Output: `capture_service.py` (LatestFrameCapture class), `color_math.py` (rgb_to_xy, build_polygon_mask, extract_region_color), and comprehensive unit tests for both.
</objective>

<execution_context>
@/home/lukas/.claude/get-shit-done/workflows/execute-plan.md
@/home/lukas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-capture-pipeline-color-extraction/02-RESEARCH.md

<interfaces>
<!-- Existing codebase contracts the executor needs -->

From Backend/main.py:
```python
from contextlib import asynccontextmanager
from fastapi import FastAPI
from database import DATABASE_PATH, close_db, init_db

@asynccontextmanager
async def lifespan(app: FastAPI):
    db = await init_db(DATABASE_PATH)
    app.state.db = db
    yield
    await close_db(db)

app = FastAPI(lifespan=lifespan)
```

From Backend/requirements.txt:
```
fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
hue-entertainment-pykit==0.9.4
aiosqlite>=0.20,<1
httpx>=0.27,<1
python-multipart>=0.0.18,<1
pydantic>=2.10,<3
pytest>=8.3,<9
pytest-asyncio>=0.24,<1
```

From Backend/tests/conftest.py (test patterns):
```python
@pytest.fixture
async def db():
    """In-memory aiosqlite connection with full schema initialized."""
    conn = await aiosqlite.connect(":memory:")
    # ... schema setup ...
    yield conn
    await conn.close()

@pytest.fixture
def app_client(tmp_path):
    """FastAPI TestClient with lifespan using a temp file DB."""
    # ... test app creation pattern ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create color_math module with TDD</name>
  <files>Backend/services/color_math.py, Backend/tests/test_color_math.py</files>
  <behavior>
    - rgb_to_xy(255, 0, 0) returns xy within Gamut C triangle (x near 0.692, y near 0.308)
    - rgb_to_xy(0, 255, 0) returns xy near green vertex (0.17, 0.7)
    - rgb_to_xy(0, 0, 255) returns xy near blue vertex (0.153, 0.048)
    - rgb_to_xy(0, 0, 0) returns D65 white point (0.3127, 0.3290) without raising
    - rgb_to_xy(255, 255, 255) returns a valid xy within Gamut C
    - _in_gamut correctly identifies points inside and outside the Gamut C triangle
    - _clamp_to_gamut moves an out-of-gamut point to nearest Gamut C edge
    - build_polygon_mask with normalized [[0,0],[0.5,0],[0.5,1],[0,1]] returns uint8 mask of shape (480,640) with 255 in left half
    - build_polygon_mask clamps coordinates at boundaries (x=1.0 does not produce index 640)
    - extract_region_color returns (r,g,b) tuple from a solid-color test frame with known mask
  </behavior>
  <action>
    1. Create `Backend/tests/test_color_math.py` with all test cases listed in behavior. Use real numpy arrays for mask/frame tests (no mocking needed for pure math). Import from `services.color_math`.

    2. Run tests — they must FAIL (module does not exist yet).

    3. Create `Backend/services/color_math.py` implementing:
       - `GAMUT_C` dict with red/green/blue vertices per Philips SDK
       - `_cross_product`, `_closest_point_on_segment`, `_in_gamut`, `_clamp_to_gamut` helper functions
       - `rgb_to_xy(r: int, g: int, b: int) -> tuple[float, float]` — normalize, gamma expand, Wide RGB D65 matrix, XYZ to xy, clamp to Gamut C. Return D65 white point for black input.
       - `build_polygon_mask(normalized_points, width=640, height=480) -> np.ndarray` — clamp coordinates with `min(1.0, max(0.0, v)) * (dim - 1)` before int conversion. Use `cv2.fillPoly`.
       - `extract_region_color(frame: np.ndarray, mask: np.ndarray) -> tuple[int, int, int]` — `cv2.mean(frame, mask=mask)`, convert BGR to RGB.

    4. Run tests — they must PASS.

    Follow exact algorithm from 02-RESEARCH.md Pattern 4 for rgb_to_xy. Follow Pattern 2 for build_polygon_mask with the clamping fix from Pitfall 4. Follow Pattern 3 for extract_region_color.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl/Backend && python -m pytest tests/test_color_math.py -x -v</automated>
  </verify>
  <done>All color math tests pass. rgb_to_xy handles all primary colors + black + white. build_polygon_mask produces correct shape with clamped coordinates. extract_region_color returns correct RGB from test frames.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create capture_service module with TDD and install opencv dependency</name>
  <files>Backend/requirements.txt, Backend/services/capture_service.py, Backend/tests/test_capture_service.py</files>
  <behavior>
    - LatestFrameCapture.__init__ stores device_path, _cap is None
    - open() creates cv2.VideoCapture with CAP_V4L2, sets MJPG fourcc at 640x480
    - open() discards first 3 frames to avoid black frame issue
    - open() with non-existent device raises RuntimeError
    - open(new_path) closes existing device before reopening
    - release() releases capture and sets _cap to None
    - release() is safe to call when _cap is already None
    - get_frame() calls asyncio.to_thread with _read_frame
    - get_frame() raises RuntimeError when _cap is None
    - _read_frame raises RuntimeError when cap.read() returns False
    - CAPTURE_DEVICE env var is readable (tested at module level)
  </behavior>
  <action>
    1. Add `opencv-python-headless>=4.10,<5` to `Backend/requirements.txt` (after existing deps, before test deps). Then run `cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl/Backend && pip install opencv-python-headless>=4.10,<5`.

    2. Create `Backend/tests/test_capture_service.py` with all test cases listed in behavior. Mock `cv2.VideoCapture` using `unittest.mock.patch`. For `get_frame`, mock `asyncio.to_thread` to call the function directly (or use a real event loop). Use `monkeypatch` for env var test.

    3. Run tests — they must FAIL (module does not exist yet).

    4. Create `Backend/services/capture_service.py` implementing `LatestFrameCapture` class:
       - `__init__(self, device_path: str = "/dev/video0")`: store path, `_cap = None`, create `self._lock = asyncio.Lock()`
       - `open(self, device_path: Optional[str] = None)`: close existing cap if any. Create `cv2.VideoCapture(path, cv2.CAP_V4L2)`. Check `isOpened()` — raise RuntimeError if False. Set MJPG fourcc, 640 width, 480 height. Log actual fourcc with struct.pack. Discard first 3 frames. Store `_device_path`.
       - `release(self)`: release cap if not None, set to None.
       - `_read_frame(self)`: check `_cap` is not None and `isOpened()`, call `cap.read()`, raise if ret is False, return frame.
       - `async get_frame(self) -> np.ndarray`: `async with self._lock: return await asyncio.to_thread(self._read_frame)`
       - `@property device_path(self) -> str`: return `_device_path`

    5. Run tests — they must PASS.

    Follow exact patterns from 02-RESEARCH.md Pattern 1. Include the asyncio.Lock per Open Question 3 in the research. Make open() non-fatal logging per Open Question 2 is NOT done here (the class raises; the lifespan in Plan 02 will handle graceful degradation).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl/Backend && python -m pytest tests/test_capture_service.py -x -v</automated>
  </verify>
  <done>opencv-python-headless is installed and in requirements.txt. All capture service tests pass. LatestFrameCapture opens V4L2 device with MJPG at 640x480, delegates cap.read to asyncio.to_thread, handles re-open and release correctly.</done>
</task>

</tasks>

<verification>
Run the full test suite to ensure no regressions from Phase 1 tests:
```bash
cd /mnt/c/Users/Lukas/IdeaProjects/HuePictureControl/Backend && python -m pytest tests/ -x -v
```
All existing Phase 1 tests plus new capture_service and color_math tests must pass.
</verification>

<success_criteria>
- `Backend/services/capture_service.py` exists with `LatestFrameCapture` class
- `Backend/services/color_math.py` exists with `rgb_to_xy`, `build_polygon_mask`, `extract_region_color`
- `opencv-python-headless>=4.10,<5` is in `Backend/requirements.txt`
- All tests in `test_capture_service.py` and `test_color_math.py` pass
- Full test suite (including Phase 1 tests) shows no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-capture-pipeline-color-extraction/02-01-SUMMARY.md`
</output>
